spring.application.name=DocTime
# Database Configuration
spring.datasource.url= jdbc:postgresql://localhost:5432/doctime_db
spring.datasource.username=postgres
spring.datasource.password=A13rj20@24


#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Hibernate (Auto-creates tables)
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Admin secret ? override with environment variable in production
# JWT
jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
jwt.expiration=36000000

# Admin credentials ? override with env variables in production
admin.email=${ADMIN_EMAIL:admin@doctime.com}
admin.password=${ADMIN_PASSWORD:Admin@1234}

spring.jpa.open-in-view=false
server.tomcat.max-threads=20
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1

